import { Injectable } from '@angular/core';
import { Docente } from './docente.model';

@Injectable()
export class CompraServicio{
    
    continuarCompra(cantidad:number, id_docente:number, id_estudiante:number){

    }

}